const UI = (() => {
  const CONTENT = document.getElementById('content');

  const initialize = () => {
    CONTENT.innerHTML = 'lalaladfsd';
  };

  return {
    initialize,
  };
})();

export default UI;